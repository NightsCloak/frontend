apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: intract-tls-staging-crt
  namespace: staging
spec:
  secretName: staging-tls
  dnsNames:
    - beta.intract.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
