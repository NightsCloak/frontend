apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nc-develop-crt
  namespace: nc-develop
spec:
  secretName: nc-dev-cert
  dnsNames:
    - nightscloak.dev
    - www.nightscloak.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer