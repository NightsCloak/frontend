apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: intract-develop-crt
  namespace: develop
spec:
  secretName: develop-tls
  dnsNames:
    - intract.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
